{
  "name": "parkgive",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:apply": "wrangler d1 execute parkgive --file=schema.sql",
    "db:apply:remote": "wrangler d1 execute parkgive --file=schema.sql --remote",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.7.4",
    "stripe": "^17.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250409.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.13.2"
  }
}
